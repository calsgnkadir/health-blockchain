# Development and CI tooling. Runtime dependencies live in requirements.txt.

# Starlette's TestClient is built on httpx and refuses to start without it.
# Pinned below 0.28: that release removed the `app=` argument the bundled
# Starlette (0.36) still passes, which breaks every TestClient-based test.
httpx==0.27.2

ruff==0.6.9
bandit==1.7.10
